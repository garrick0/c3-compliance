/**
 * RuleType - Types of compliance rules
 */

export enum RuleType {
  DEPENDENCY = 'dependency',
  NAMING = 'naming',
  STRUCTURE = 'structure',
  COMPLEXITY = 'complexity',
  CUSTOM = 'custom'
}
